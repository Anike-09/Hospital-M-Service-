services:
  hospital-service:
    image: ${DOCKER_USERNAME}/hospital-management-service:${BUILD_NUMBER}
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    restart: unless-stopped
